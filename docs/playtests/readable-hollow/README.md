# Readable Hollow playtest evidence

Checkpoint `0.1.2-readable-hollow`, captured 2026-07-20 at 1280×720.

| State | Evidence | Reading |
|---|---|---|
| Before | `before-native.png` | Full-height foreground walls consume the combat aperture. |
| Native after | `after-native.png` | The ranger and elite remain legible behind a low solid boundary. |
| Web after | `after-web.png` | The exported renderer preserves room, actor, enemy, and UI readability. |

The browser receipt completed `title → town → world → complete` in 11.4 seconds
with an empty warning/error/exception ledger. The native gate deliberately
reverses the camera after the cut to prove that every wall restores.
