# Spec 64 implementation notes — Sure Road

## Decision record

The interaction scanner still chooses the nearest eligible physical Area. The
checkpoint does not replace that player-facing rule. Instead, settled encounter
props implement their existing `is_used()` seam, while the release driver may
arm a one-shot expected target inside the Player's real interaction boundary.
The expectation is consumed on that input edge. Invalid, missing, or retargeted
objects return a refusal receipt and perform no alternative interaction.

The long journey also exposed two release-only gaps repaired within the same
proof boundary: Rich Seams now applies to renewable Wildgold, and the physical
Lodge/status projection is available to Threadstep training. The Web harness
validates structured support, combat/gather, purchase, economy, and interaction
receipts instead of obsolete training aliases.

## Verification

- Canonical native gate: 845/845.
- Focused Fire world: 10/10; intentional wrong chain included.
- Focused Fire encounter: 17/17; every banked chain becomes inert.
- Focused Pale Oath world: 17/17; relieved bells become scanner-inert.
- Focused far-return/status gate: 6/6.
- Export audit: 112/112 release resources; PCK 105,898,896 bytes.
- Fresh rendered Chromium journey: 362.069 seconds through full D7 completion.
- Final harness: passed; browser warnings/errors/assertions/exceptions: 0.
- Target viewport: 16:9 exported browser canvas.
