# Godot 4+ build artifacts
.godot/
.import/

# Web / platform export output — rebuildable, large (36MB+ WASM)
build/

# Mono / C# (not used today, harmless)
.mono/
data_*/
mono_crash.*.json

# OS noise
.DS_Store
